import Vue from 'vue'

import {
  Tab,
  Tabs,
  Area,
  Popup,
  Swipe,
  SwipeItem,
  Step,
  Steps,
  Stepper,
  PullRefresh,
  List,
  Toast,
  Field,
  Dialog,
  RadioGroup,
  Radio,
  PasswordInput,
  NumberKeyboard,
  ActionSheet,
  Uploader,
  Cell,
  CellGroup,
  Grid,
  GridItem,
  Row,
  Col,
  Icon,
  DropdownMenu,
  DropdownItem,
  Form,
  Button,
  Tag,
  Lazyload,
  Picker,
  Search,
  Progress,
  Loading 
} from 'vant'

Vue.use(Tab)
  .use(Tabs)
  .use(Area)
  .use(Popup)
  .use(Swipe)
  .use(SwipeItem) // 轮播图
  .use(Step)
  .use(Steps) // 步骤条
  .use(Stepper)
  .use(PullRefresh)
  .use(List) // 上拉加载
  .use(Toast) // 提示框
  .use(Field)
  .use(Dialog) // 遮罩层
  .use(RadioGroup) // 单选
  .use(Radio)
  .use(PasswordInput) // 密码输入框
  .use(NumberKeyboard) // 数字键盘
  .use(ActionSheet) // 上拉框
  .use(Uploader) // 图片上传
  .use(Cell)
  .use(CellGroup)
  .use(Grid)
  .use(GridItem)
  .use(Row)
  .use(Col)
  .use(Icon)
  .use(DropdownMenu)
  .use(DropdownItem)
  .use(Form)
  .use(Button)
  .use(Tag)
  .use(Lazyload)
  .use(Picker)
  .use(Search)
  .use(Progress)
  .use(Loading)
